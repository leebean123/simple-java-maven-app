pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'build sucesss'
      }
    }
    stage('test') {
      steps {
        echo 'test-sucesss'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploy sucess'
      }
    }
  }
}